server:
  port: 8070

spring:
  application:
    name: recommendor
  datasource:
    url: jdbc:postgresql://localhost:5432/bookrecommendationdb
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 12345
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          batch_size: 1000
        order_updates: true
        order_inserts: true
jwt:
  secret: uifdjhwerutw37348798((&*()_^%^&&(&%&^*(ouDIGHHJGEJK


